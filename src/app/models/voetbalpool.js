(function() {

  VP.Models.PoolPlayer = Backbone.Model.extend({
  });

  VP.Models.Team = Backbone.Model.extend({
  });

  VP.Models.Game = Backbone.Model.extend({
  });

  VP.Models.Prediction = Backbone.Model.extend({
  });

})();